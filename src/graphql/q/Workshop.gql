#import '../f/Workshop.gql'
#import '../f/Lesson.gql'

query($workshop: String!, $human: String!) {
  workshop(workshop: $workshop, human: $human) {
    ...Workshop
    lessonSet {
      ...Lesson
    }
  }
}
