query($lesson: Int!, $workshop: String!, $human: String!) {
  lessonSlides(lesson: $lesson, workshop: $workshop, human: $human) {
    id
  }
  lesson(lesson: $lesson, workshop: $workshop, human: $human) {
    id
    name
  }
}
